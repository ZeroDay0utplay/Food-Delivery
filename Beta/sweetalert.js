import swal from 'sweetalert';
const express = require("express");
const bodyParser = require("body-parser");
const fs = require("fs");

const app = express();

swal("Hello world!");



app.listen(3000, () => {});


